# WIDGET: Number JSON Api

This number widget expects a JSON endpoint that publishes a JSON object in the form:

```Javascript
{
  "count": <Integer>
}
```

This will render a number on the screen equal to value of "count".
